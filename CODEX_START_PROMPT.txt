Open this repository as the current working project.

Read AGENTS.md, CODEX_HANDOVER.md and README.md before changing anything. Treat AGENTS.md as repository instructions.

First:

1. Inspect the repository and current git state.
2. Run npm ci, npm run lint and npm test.
3. Report the exact baseline, including any failures, without hiding or skipping tests.
4. Confirm that the built-in catalogue still contains 214 mode-specific objectives, 203 distinct canonical command strings, 9 CLI modes and 25 topics.

Then help me implement the next requested change as one vertically complete slice. Do not add dead navigation, fake statistics, placeholder achievements or incomplete game modes. Preserve deterministic command validation, the timed-round answer-reveal policy, local progress, UK English and all security controls.

My next requested change is:

[REPLACE THIS LINE WITH THE NEXT FEATURE OR FIX]

Before coding, turn that request into a short acceptance checklist. Continue through implementation, automated tests, documentation and final verification without stopping after the plan.
